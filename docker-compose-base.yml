---
# This is the Base Compose template. It contains the reusable components
# for running DMS Lite
version: "3.3"
services:
  web:
    build:
      dockerfile: Dockerfile
      context: ./dms-lite
    image: dms-lite-web:${DMSLITEVERSION}
    restart: "always"
    depends_on:
      - mariadb
    ports:
      - "3000:3000"
    links:
      - mariadb
    environment:
      MYSQL_DATABASE: "${MYSQL_DATABASE}"
      MYSQL_USER: "${MYSQL_USER}"
      MYSQL_PASSWORD: "${MYSQL_PASSWORD}"
  mariadb:
    image: lscr.io/linuxserver/mariadb@sha256:e667280bba05043c53b1a0822194273f8de87c7fcb0b9a9ba3665db9a7fef50d
    hostname: mariadb
    restart: "always"
    volumes:
      - /var/lib/mysql:/config
    environment:
      PUID: 1000
      PGID: 1000
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD}"
      MYSQL_DATABASE: "${MYSQL_DATABASE}"
      MYSQL_USER: "${MYSQL_USER}"
      MYSQL_PASSWORD: "${MYSQL_PASSWORD}"
